pipeline {
 
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/saravanan2024chennai/sonartest.git' 
            }
        }
        stage('Build') {
            steps {
                sh 'mvn clean compile'
            }
        }
        
    }
}
